# コンセプト検証版（参考）

TogoCoord の最初のコンセプト検証実装です。現在の実装（`core/`、`ingest/`、`service/`）は、これを参考に一から作り直したもので、ここのコードは使っていません。

| ディレクトリ | 内容 |
|---|---|
| `html/` | Web UI（ヒトとマウスの一部で検証） |
| `sparqlist/` | 変換 API（SPARQList。Ensembl REST、TogoWS、UniParc などの外部 API を利用） |
